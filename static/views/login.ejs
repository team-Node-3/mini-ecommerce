<!DOCTYPE html>
<html lang="en">
  <head>

    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>로그인</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <!-- 나의 스타일 추가 -->
    <link rel="stylesheet" href="../styles/login.css">

    <!-- font awesome -->
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.0.8/css/all.css"
    />

    <!-- j query -->

    <script src="https://code.jquery.com/jquery-3.6.3.js" integrity="sha256-nQLuAZGRRcILA+6dMBOvcRh5Pe310sBpanc6+QBmyVM=" crossorigin="anonymous"></script>
    
  </head>
  <body class="text-center">
    
    <!--  html 전체 영역을 지정하는 container -->
    <div id="container">
      <!--  login 폼 영역을 : loginBox -->
      <div id="loginBox">
      
        <!-- 로그인 페이지 타이틀 -->
        <div id="loginBoxTitle">슈즈 싹쓰리</div>
        <!-- 아이디, 비번, 버튼 박스 -->
        <div id="inputBox">
          <div class="input-form-box"><span class="input">아이디</span><input id="nickname" type="text" name="uid" class="form-control"></div>
          <div class="input-form-box"><span class="input">비밀번호</span><input id="password" type="password" name="upw" class="form-control"></div>
          <div class="button-login-box" >
            <button type="button" class="btn btn-primary btn-xs" style="width:100%" onclick="login()">로그인</button>
          </div>
          <button type="button" class="btn btn-primary btn-xs" style="width:100%" onclick="location.href='/signUp'">회원가입</button>
          </div>
          <div className="social-login-div">
            <a href={'http://localhost:3000/auth/github'}>Log in with Github</a>
          </div>
      </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <script>
      function login() {
        let nickname = $("#nickname").val();
        let password = $("#password").val();
    
        if (!nickname || !password) {
          return customAlert("이메일과 패스워드를 넣어주세요");
        }
    
        $.ajax({
          type: "POST",
          url: "/auth/login",
          data: {
            nickname: nickname,
            password: password,
          },
          success: function (response) {
            customAlert("로그인 완료!");
            //로그인구현되면 변경해야함~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
            window.location.replace("/user"); //사장인지 유저인지 구분해서
          },
          error: function (response) {
            customAlert("정확한 내용을 입력해 주세요");
          },
        });
      }
    
      function customAlert(text) {
        $("#alertText").text(text);
        $("#alertModal").modal("show");
      }
    </script>
  </body>
</html>